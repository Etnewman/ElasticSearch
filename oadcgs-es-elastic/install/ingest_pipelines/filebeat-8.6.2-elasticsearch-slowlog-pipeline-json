{
    "description" : "Pipeline for parsing the Elasticsearch slow logs in JSON format.",
    "on_failure" : [
      {
        "set" : {
          "field" : "error.message",
          "value" : "{{ _ingest.on_failure_message }}"
        }
      }
    ],
    "processors" : [
      {
        "pipeline" : {
          "if" : "!ctx.message.contains(\"ecs.version\")",
          "name" : "filebeat-8.6.2-elasticsearch-slowlog-pipeline-json-7"
        }
      },
      {
        "pipeline" : {
          "if" : "ctx.message.contains(\"ecs.version\")",
          "name" : "filebeat-8.6.2-elasticsearch-slowlog-pipeline-json-8"
        }
      }
    ]
  }
